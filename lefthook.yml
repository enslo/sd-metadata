pre-commit:
  parallel: true
  commands:
    lint:
      run: npm run lint
    typecheck:
      run: npm run typecheck
    markdown:
      glob: "*.md"
      run: npx markdownlint --fix {staged_files}
