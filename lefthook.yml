pre-commit:
  parallel: true
  commands:
    lint:
      run: npm run lint
    typecheck:
      run: npm run typecheck
    markdown:
      glob: "*.md"
      run: npm run markdownlint -- {staged_files}
